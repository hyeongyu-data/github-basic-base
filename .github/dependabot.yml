version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
  # 언어 패키지 매니저를 쓰면 추가하세요 (npm / pip / gomod / cargo / bundler 등)
  # - package-ecosystem: npm
  #   directory: "/"
  #   schedule:
  #     interval: weekly
